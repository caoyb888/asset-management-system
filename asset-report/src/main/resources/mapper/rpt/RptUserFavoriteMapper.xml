<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.asset.report.mapper.rpt.RptUserFavoriteMapper">

    <!-- 用户收藏列表，按 sort_order 升序 -->
    <select id="selectByUserId" resultType="com.asset.report.entity.RptUserFavorite">
        SELECT id, user_id, report_id, report_code, report_name, route_path,
               category, sort_order, quick_access, created_by, created_at,
               updated_by, updated_at, is_deleted
        FROM rpt_user_favorite
        WHERE user_id = #{userId}
          AND is_deleted = 0
        ORDER BY sort_order ASC, id ASC
    </select>

    <!-- 检查是否已收藏 -->
    <select id="countByUserAndCode" resultType="int">
        SELECT COUNT(1)
        FROM rpt_user_favorite
        WHERE user_id   = #{userId}
          AND report_code = #{reportCode}
          AND is_deleted = 0
    </select>

    <!-- 更新单条排序（仅允许本人操作） -->
    <update id="updateSortOrder">
        UPDATE rpt_user_favorite
        SET sort_order = #{sortOrder}
        WHERE id      = #{id}
          AND user_id = #{userId}
          AND is_deleted = 0
    </update>

</mapper>
