# 基础数据管理模块 — 未完成功能清单

> 更新日期：2026-02-23
> 对比依据：`docs/基础数据管理模块数据库设计.md` + `docs/基础数据管理模块_技术分析报告.docx`
> 当前进度：基础 CRUD + 子资源 + 通知/资讯模块已完成（约 85%），剩余图片上传、部分前端操作入口、定时任务等

---

## 完成状态总览

| 子模块 | 基础CRUD | 子资源管理 | 业务逻辑 | 文件上传 | 导入导出 | 完成度 |
|--------|:--------:|:----------:|:--------:|:--------:|:--------:|:------:|
| 项目管理 | ✅ | ✅ | — | ❌ | — | 85% |
| 楼栋管理 | ✅ | — | ✅ | ❌ | — | 80% |
| 楼层管理 | ✅ | — | ✅ | ❌ | — | 80% |
| 商铺管理 | ✅ | — | ✅ | ❌ | ❌ | 75% |
| 品牌管理 | ✅ | ✅ | — | — | ✅后端/❌前端 | 85% |
| 商家管理 | ✅ | ✅ | ✅后端/❌前端 | ✅ | ✅后端/❌前端 | 85% |
| 通知公告 | ✅ | — | ✅ | — | — | 80% |
| 新闻资讯 | ✅ | — | ✅ | — | — | 80% |

---

## 一、项目管理

### 1.1 已完成
- [x] 项目列表查询（分页 + 多条件筛选）
- [x] 项目新增 / 编辑 / 删除（逻辑删除）/ 详情
- [x] 财务联系人列表、新增、编辑、删除（后端 + 前端 Drawer）
- [x] 银行账号列表、新增、编辑、删除（后端 + 前端 Drawer）
- [x] 合同甲方信息查询、保存（后端 + 前端 Drawer）

### 1.2 未完成

| 优先级 | 功能 | 接口 | 说明 |
|--------|------|------|------|
| P1 | 项目图片上传 | `POST /base/projects/{id}/images` | image_urls 为 JSON 数组 |
| P1 | 项目图片删除 | `DELETE /base/projects/{id}/images/{index}` | 按下标删除 |
| P1 | 前端图片上传组件 | — | 嵌入详情 Drawer 基本信息 Tab |

---

## 二、楼栋管理

### 2.1 已完成
- [x] 楼栋列表查询（分页 + 按项目筛选）
- [x] 楼栋新增 / 编辑 / 删除 / 详情
- [x] 删除前关联校验（存在楼层或商铺时拒绝删除）

### 2.2 未完成

| 优先级 | 功能 | 接口 | 说明 |
|--------|------|------|------|
| P1 | 楼栋平面图上传 | `POST /base/buildings/{id}/image` | 更新 image_url 字段 |
| P1 | 前端楼栋图上传/预览 | — | 新增/编辑弹窗内嵌 |

---

## 三、楼层管理

### 3.1 已完成
- [x] 楼层列表查询（分页 + 按项目/楼栋筛选）
- [x] 楼层新增 / 编辑 / 删除 / 详情
- [x] 删除前关联校验（存在商铺时拒绝删除）

### 3.2 未完成

| 优先级 | 功能 | 接口 | 说明 |
|--------|------|------|------|
| P1 | 楼层平面图上传 | `POST /base/floors/{id}/image` | 更新 image_url 字段 |
| P1 | 前端楼层图上传/预览 | — | 新增/编辑弹窗内嵌 |

---

## 四、商铺管理

### 4.1 已完成
- [x] 商铺列表查询（分页 + 多条件筛选）
- [x] 商铺新增 / 编辑 / 删除 / 详情
- [x] 商铺拆分接口（后端 + 前端 Dialog，面积守恒校验）
- [x] 商铺合并接口（后端 + 前端 Dialog，面积守恒校验）

### 4.2 未完成

| 优先级 | 功能 | 接口 | 说明 |
|--------|------|------|------|
| P1 | 商铺 Excel 批量导入 | `POST /base/shops/import` | 含数据校验和错误行提示 |
| P2 | 商铺导入模板下载 | `GET /base/shops/template` | 返回标准 Excel 模板 |
| P1 | 前端导入对话框 | — | 文件上传 + 错误回显 |

---

## 五、品牌管理

### 5.1 已完成
- [x] 品牌列表查询（分页 + 多条件筛选）
- [x] 品牌新增 / 编辑 / 删除 / 详情
- [x] 品牌联系人列表、新增、编辑、删除（后端 + 前端 Dialog）
- [x] Excel 批量导入（后端接口 `POST /base/brands/import`）
- [x] 导入模板下载（后端接口 `GET /base/brands/template`）

### 5.2 未完成

| 优先级 | 功能 | 说明 |
|--------|------|------|
| P1 | 前端 Excel 导入对话框 | 文件上传 + 错误行回显，嵌入品牌列表页 |
| P2 | 前端模板下载按钮 | 触发 `GET /base/brands/template` |

---

## 六、商家管理

### 6.1 已完成
- [x] 商家列表查询（分页 + 多条件筛选，含审核状态筛选）
- [x] 商家新增 / 编辑 / 删除 / 详情
- [x] 联系人 CRUD（后端 + 前端 Drawer Tab）
- [x] 诚信记录 CRUD（后端 + 前端 Drawer Tab）
- [x] 开票信息 CRUD（后端 + 前端 Drawer Tab）
- [x] 附件上传/删除（后端 + 前端 Drawer Tab，依赖 asset-file 服务）
- [x] 商家审核接口（`PUT /base/merchants/{id}/audit`，通过/驳回）
- [x] Excel 批量导入（后端接口 `POST /base/merchants/import`）
- [x] 导入模板下载（后端接口 `GET /base/merchants/template`）

### 6.2 未完成

| 优先级 | 功能 | 说明 |
|--------|------|------|
| P1 | 前端审核操作按钮 | 列表行内增加「通过/驳回」操作，弹窗填写驳回原因 |
| P1 | 前端 Excel 导入对话框 | 文件上传 + 错误行回显 |
| P2 | 前端模板下载按钮 | 触发 `GET /base/merchants/template` |

#### 安全规范备注
```
- id_card 字段：SM4 加密存储，列表接口返回脱敏值（110***1234）
- 详情接口查看明文需特殊权限：merchant:sensitive:view
- 所有查询 id_card 的操作需记录审计日志
```

---

## 七、通知公告管理

### 7.1 已完成
- [x] 公告列表查询（分页 + 按标题/类型/状态/时间筛选）
- [x] 新增 / 编辑 / 删除公告（后端 + 前端）
- [x] 发布公告（`PUT /base/notices/{id}/publish`，写入发布时间）
- [x] 下架公告（`PUT /base/notices/{id}/unpublish`）
- [x] 前端列表页（路由 `/base/notices`，含发布/下架操作按钮）

### 7.2 未完成

| 优先级 | 功能 | 接口 | 说明 |
|--------|------|------|------|
| P1 | 定时发送 | 发布接口支持 `scheduled_time` 触发 | 需配合 XXL-Job 定时任务扫描 |
| P2 | 标记已读 | `POST /base/notices/{id}/read` | 写 biz_notice_read |
| P2 | 已读统计 | `GET /base/notices/{id}/read-stats` | 返回已读/未读人数 |

---

## 八、新闻资讯管理

### 8.1 已完成
- [x] 资讯列表查询（分页 + 按标题/分类/状态筛选）
- [x] 发布 / 编辑 / 删除资讯（后端 + 前端）
- [x] 上架（`PUT /base/news/{id}/publish`）/ 下架（`PUT /base/news/{id}/unpublish`）
- [x] 前端列表页（路由 `/base/news`，含上架/下架操作按钮）

### 8.2 未完成

| 优先级 | 功能 | 接口 | 说明 |
|--------|------|------|------|
| P1 | 分类管理 | `GET/POST /base/news/categories` | 新闻/政策/招商/服务指南，目前为枚举硬编码 |

---

## 优先级汇总

### P1 — 重要功能，建议优先完成

1. 图片上传功能（项目封面图 / 楼栋图 / 楼层图）
2. 商家前端审核操作按钮（后端接口已就绪）
3. 品牌/商家/商铺 Excel 导入前端对话框（品牌/商家后端已就绪，商铺后端尚未实现）
4. 商铺 Excel 批量导入后端接口

### P2 — 锦上添花，最后完成

5. 各模块 Excel 导入模板下载前端按钮
6. 通知公告定时发送（XXL-Job 集成）
7. 通知公告已读/未读追踪
8. 新闻资讯分类管理

---

## 待办任务列表

- [ ] **[P1]** 实现项目/楼栋/楼层图片上传接口及前端组件
- [ ] **[P1]** 前端商家列表增加审核操作按钮（通过/驳回）
- [ ] **[P1]** 前端品牌/商家 Excel 导入对话框
- [ ] **[P1]** 商铺 Excel 批量导入（后端接口 + 前端对话框）
- [ ] **[P2]** 前端各模块模板下载按钮
- [ ] **[P2]** 通知公告定时发送（XXL-Job 定时扫描 scheduled_time）
- [ ] **[P2]** 通知公告已读追踪（biz_notice_read）
- [ ] **[P2]** 新闻资讯分类管理接口
